#!/bin/bash

# This script was used to merge data from separate CSV files,
# into a single CSV file.
# It is NOT needed if your data is already formatted 
# as a single CSV file.

python3 ./merge.py --dir "$PROJECT_DIR/data/" $ 